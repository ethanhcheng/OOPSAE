package Worker;

import javax.swing.*;

public class StockClerk extends JFrame {
    public StockClerk() {
        setTitle("Stock Clerk Interface");
        setSize(400, 200);
        setDefaultCloseOperation(JFrame.DISPOSE_ON_CLOSE);
        setLocationRelativeTo(null);

        JLabel label = new JLabel("Stock Clerk Interface (Not Implemented)");
        add(label);

        setVisible(true);
    }
}

